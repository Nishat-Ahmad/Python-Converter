#include <iostream>
#include <vector>
#include <string>

using namespace std;

string replaceWords(vector<string>& dictionary, string sentence) {
    string s;
     while (getline(sentence, s, ' ')) {
 
        // store token string in the vector
        v.push_back(s);
    }
    for(int i = 0;i < )
}